---
# Source: rgs-v2/templates/tests/test-connection.yaml
apiVersion: v1
kind: Pod
metadata:
  name: "rgs-v2-test-connection"
  labels:

    helm.sh/chart: rgs-v2-1.0.2
    app.kubernetes.io/name: rgs-v2
    app.kubernetes.io/instance: rgs-v2
    app.kubernetes.io/managed-by: Helm
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['rgs-v2:3000']
  restartPolicy: Never
